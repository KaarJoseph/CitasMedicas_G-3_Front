export interface MedicoPersonal {
    id: number;
    med_per_nombre_completo: string;
    med_per_credenciales: string;
    med_per_especialidades: string;
    med_per_horarios_consulta: string;
    med_per_informacion_contacto: string;
  }
  